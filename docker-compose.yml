services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: library-backend

    env_file:
      - .env

    ports:
      - "${APP_PORT}:8081"

    volumes:
      - ./backend:/app

    working_dir: /app

    command: go run ./cmd/api